<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@drawable/style_toolbar"
    android:backgroundTint="@color/reading_toolbar_color"
    android:elevation="4dp">

    <ImageView
        android:id="@+id/backBtn"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentStart="true"
        android:layout_centerInParent="true"
        android:layout_margin="12dp"
        android:src="@drawable/ic_baseline_arrow_back_ios_24"
        app:tint="@color/to_right_color" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/container2"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/titleTv"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:ellipsize="end"
            android:lines="1"
            android:maxLines="1"
            android:text="Как стать умным ?"
            android:textAlignment="center"
            android:textColor="@color/to_right_color"
            android:textSize="18sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintWidth_percent="0.6" />

    </androidx.constraintlayout.widget.ConstraintLayout>


    <com.like.LikeButton
        android:id="@+id/likeBtn"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentEnd="true"
        android:layout_centerInParent="true"
        android:layout_marginStart="12dp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="12dp"
        android:layout_marginBottom="12dp"
        app:anim_scale_factor="2"
        app:circle_end_color="#D3C75F"
        app:circle_start_color="#DDCE4B"
        app:dots_primary_color="#E91E63"
        app:dots_secondary_color="@color/light_green"
        app:icon_size="25dp"
        app:icon_type="Star"
        app:is_enabled="true"
        app:like_drawable="@drawable/ic_baseline_favorite_24"
        app:liked="false"
        app:unlike_drawable="@drawable/ic_baseline_favorite_border_24" />

</RelativeLayout>